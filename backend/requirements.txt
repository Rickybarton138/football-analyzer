# Football Match Analyzer - Backend Requirements

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
aiofiles>=23.2.1

# Data Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Computer Vision & ML
opencv-python-headless>=4.8.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Deep Learning - detection & tracking pipeline
torch>=2.1.0
ultralytics>=8.3.0

# Roboflow supervision - detection format bridge & tracking
supervision>=0.25.0

# SAHI - sliced inference for small object (ball) detection
sahi>=0.11.0

# SigLIP team classification (vision embeddings + clustering)
transformers>=4.40.0
umap-learn>=0.5.5

# Roboflow SDK - dataset download for fine-tuning
roboflow>=1.1.0

# OCR for jersey number recognition (choose one)
# easyocr>=1.7.0  # Recommended - easier setup
# paddleocr>=2.7.0  # Alternative - may have better accuracy

# Video Processing
# Note: FFmpeg must be installed separately on the system
ffmpeg-python>=0.2.0

# Database
aiosqlite>=0.19.0

# HTTP Client (for cloud inference)
httpx>=0.26.0
aiohttp>=3.9.0

# Web scraping
beautifulsoup4>=4.12.0

# Data Processing
pandas>=2.0.0

# Optional: Redis caching
# redis>=5.0.0

# AI Coaching (Claude + OpenAI Vision)
anthropic>=0.40.0
openai>=1.50.0

# Auth
python-jose[cryptography]>=3.3.0

# System
psutil>=5.9.0

# Development
python-dotenv>=1.0.0
